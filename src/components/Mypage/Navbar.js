import TitleBar from "../../components/TitleBar";

const Navbar = () => {
  return (
    <TitleBar>
      <span style={{ color: "var(--green1)" }}>마</span>
      <span style={{ color: "var(--green2)" }}>이</span>
      <span style={{ color: "var(--green3)" }}>페이지</span>
    </TitleBar>
  );
};
export default Navbar;
